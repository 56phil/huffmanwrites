# Quotations that predate the citation rule (CLAUDE.md).
#
# EMPTY, and meant to stay that way. On 2026-09-20 the 68 grandfathered
# attributions were all repaired: each now names its work and carries a URL
# that actually contains the quoted wording. check-quotes.py passes with no
# exemptions, and --online verified all 52 epigraph links.
#
# The list is kept, not deleted, because the mechanism is still the right one
# for a genuinely pre-rule case. If you must exempt one:
#   - key it by file + a hash of the quoted text, so editing the quotation
#     withdraws its exemption and the rule applies on next touch;
#   - never hand-edit; regenerate with
#     scripts/check-quotes.py --update-baseline
#
# Prefer repairing the citation. An exemption is a promise to look later, and
# 68 of them went unlooked-at for months.
#
# file	sha1_12	attribution
